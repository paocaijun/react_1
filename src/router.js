import {
    Router,
    Route,
    IndexRoute,
    hashHistory,
    Redirect
} from 'react-router'
import index from '@/pages/'
export const () => (
    <Router history={hashHistory}>
    <route path='/' component=''></route>
    </Router>

)